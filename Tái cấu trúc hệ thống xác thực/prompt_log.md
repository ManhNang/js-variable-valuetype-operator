prompt 1:
Giải thích cơ chế Type Coercion trong JS qua các ví dụ và phân tích tại sao dùng == trong kiểm tra điều kiện lại có thể dẫn đến lỗ hổng Logic/Security

prompt 2:
Liệt kê bảng so sánh kết quả giữa == và === cho các trường hợp dữ liệu 'falsy'. Làm thế nào để mình nhận diện nhanh những chỗ cần thay thế sang === mà không làm hỏng logic hiện tại của chương trình?

prompt 3:
Dưới đây là đoạn mã nguồn cũ của mình. Đừng viết lại code, hãy chỉ ra cho mình 2 điểm yếu logic

prompt 4:
Mình có các khối if-else lồng nhau rất phức tạp. Hãy hướng dẫn mình cách dùng kỹ thuật Guard Clauses (trả về sớm) và toán tử logic để làm phẳng cấu trúc này mà không cần viết lại cả hàm cho mình