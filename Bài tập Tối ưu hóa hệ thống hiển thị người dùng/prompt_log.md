prompt 1:
So sánh việc dùng toán tử 3 ngôi và if-else trong JavaScript. Khi nào dùng 3 ngôi sẽ khiến code khó đọc hơn? Cho ví dụ về một trường hợp lồng nhau

prompt 2:
Tôi có một đoạn mã if-else dài 10 dòng chỉ để kiểm tra quyền truy cập và gán thông báo. Hãy gợi ý cách dùng Ternary hoặc Logic Operator để đưa nó về tối đa 3 dòng mà vẫn giữ nguyên logic nghiệp vụ

prompt 3:
Trong biểu thức result = data && data.name, AI hãy giải thích tại sao cách viết này giúp tránh lỗi 'Cannot read property of undefined'. Biểu thức sẽ dừng lại ở đâu nếu data bị null?